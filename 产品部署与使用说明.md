# 灵山胜境 AI 数字导游系统部署与使用说明

> 比赛上传配套文件  
> 源码仓库：`https://github.com/Zz18studying/Trapezius-driven-development-guide.git`

本文档用于说明如何从 GitHub 获取完整源码、配置运行环境、启动产品服务，并完成比赛演示路径。项目包含游客端 Web、Vue 管理后台、FastAPI 后端、RAG 知识库、SQLite 业务数据库、ChromaDB 向量库、百度语音服务和 Live2D 数字人资源。

## 1. 源码获取

本项目全部源码以 GitHub 仓库为准：

```bash
git clone https://github.com/Zz18studying/Trapezius-driven-development-guide.git
cd Trapezius-driven-development-guide
```

如本地已经存在该仓库，可执行：

```bash
git remote -v
git pull origin main
```

当前项目推荐将仓库完整目录作为比赛源码提交，不要只提交单个前端或后端目录。以下目录均属于产品源码或运行资源：

```text
backend/          FastAPI 后端服务
admin-frontend/   Vue 3 管理后台
web/              游客端静态页面
scripts/          知识库构建与检索测试脚本
data/             原始景区数据
models/           Live2D 数字人模型资源
README.md         比赛上传最终版说明
产品部署与使用说明.md
```

## 2. 推荐运行环境

### 2.1 基础环境

| 环境 | 建议版本 | 说明 |
| --- | --- | --- |
| 操作系统 | Ubuntu 20.04+ / Windows 10+ | 服务器部署推荐 Linux |
| Python | 3.10+ | 运行 FastAPI 后端与知识库脚本 |
| Node.js | 20.19+ 或 22.12+ | 运行 Vue 管理端 |
| ffmpeg | 稳定版本 | 语音识别时将浏览器音频转换为 PCM |
| Git | 任意稳定版本 | 拉取源码 |

### 2.2 推荐部署路径

当前后端部分静态资源路径按 Linux 服务器部署方式配置，推荐将项目部署到：

```text
/var/www/Trapezius-driven-development-guide
```

如果部署在其他路径，需要检查并按实际情况调整以下文件中的路径配置：

- `backend/main.py`
- `backend/routers/voice.py`
- `backend/routers/knowledge.py`
- `backend/services/document_indexer.py`

## 3. 后端部署

### 3.1 安装 Python 依赖

在项目根目录执行：

```bash
cd backend
pip install -r requirements.txt
```

### 3.2 配置环境变量

完整体验 AI 问答和语音能力，需要配置以下环境变量：

```bash
export DEEPSEEK_API_KEY="你的 DeepSeek API Key"
export BAIDU_API_KEY="你的百度智能云 API Key"
export BAIDU_SECRET_KEY="你的百度智能云 Secret Key"
```

Windows PowerShell 可使用：

```powershell
$env:DEEPSEEK_API_KEY="你的 DeepSeek API Key"
$env:BAIDU_API_KEY="你的百度智能云 API Key"
$env:BAIDU_SECRET_KEY="你的百度智能云 Secret Key"
```

说明：

- 未配置 `DEEPSEEK_API_KEY` 时，AI 生成回答不可用。
- 未配置百度语音密钥时，文本问答仍可使用，ASR/TTS 会返回未配置提示。
- 语音识别依赖 `ffmpeg`，本机需要能在命令行直接执行 `ffmpeg`。

### 3.3 初始化数据库

后端启动时会自动调用数据库初始化逻辑，创建必要的 SQLite 表。也可以手动执行：

```bash
cd backend
python init_db.py
```

业务数据库文件：

```text
backend/data.db
```

## 4. 知识库构建

首次运行或更新景区资料后，需要构建 RAG 知识库。回到项目根目录，依次执行：

```bash
python scripts/01_extract_and_merge_data.py
python scripts/02_generate_faq.py
python scripts/03_build_vector_db.py
python scripts/04_test_retrieval.py
```

各脚本作用：

| 脚本 | 作用 |
| --- | --- |
| `01_extract_and_merge_data.py` | 抽取并整理景区结构化资料和知识单元 |
| `02_generate_faq.py` | 基于规则生成游客常见问答 |
| `03_build_vector_db.py` | 将 FAQ 与知识块写入 ChromaDB |
| `04_test_retrieval.py` | 测试知识库检索效果 |

知识库核心能力：

- ChromaDB 向量检索。
- 景点名精确兜底。
- 问题扩展与关键词重排。
- FAQ 与原文知识块双索引。

## 5. 启动后端服务

在 `backend/` 目录执行：

```bash
python main.py
```

默认服务地址：

```text
http://127.0.0.1:8000
```

接口文档：

```text
http://127.0.0.1:8000/docs
```

后端启动时会执行：

- 初始化 SQLite 数据库。
- 预热 RAG 服务。
- 预热 DeepSeek LLM 服务。
- 清理旧音频文件。
- 启动热点话题缓存定时任务。

## 6. 游客端使用

游客端文件位于：

```text
web/
```

主要页面：

| 页面 | 文件 | 用途 |
| --- | --- | --- |
| 首页 | `web/index.html` | 景区展示、热门景点、路线入口、数字人入口 |
| AI 导游 | `web/chat.html` | 游客与“小灵”进行文本/语音问答 |
| 路线推荐 | `web/routes.html` | 查看主题游览路线 |
| 景点列表 | `web/spots.html` | 查看景点卡片和评分 |
| 景点详情 | `web/spot-detail.html` | 查看景点介绍、语音讲解和评价 |

如果项目部署在推荐路径并由后端挂载静态文件，可直接访问：

```text
http://服务器IP:8000/
http://服务器IP:8000/chat.html
http://服务器IP:8000/routes.html
http://服务器IP:8000/spots.html
```

游客端通过相对路径请求后端接口，例如 `/api/chat/ask`、`/api/voice/tts`、`/api/spots/ratings`。因此建议游客端和后端部署在同一域名或通过 Nginx 转发 `/api/`。

## 7. 管理端部署与使用

管理端位于：

```text
admin-frontend/
```

### 7.1 安装依赖

```bash
cd admin-frontend
npm install
```

### 7.2 本地开发启动

```bash
npm run dev
```

默认端口：

```text
http://127.0.0.1:5173/admin/
```

注意：当前 `admin-frontend/vite.config.js` 中 `/api` 代理目标为远程后端：

```text
http://110.42.246.141:8000
```

如需本地联调，请将代理目标改为本地后端：

```js
target: 'http://127.0.0.1:8000'
```

### 7.3 构建生产版本

```bash
npm run build
```

构建产物默认位于：

```text
admin-frontend/dist/
```

部署时可由 Nginx 托管 `dist/`，并将 `/api/` 请求反向代理到 FastAPI 后端。

## 8. 核心接口

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/chat/ask` | POST | AI 导游问答 |
| `/api/chat/search` | POST | 仅检索知识库 |
| `/api/chat/session/init` | GET | 初始化或复用游客会话 |
| `/api/chat/clear` | POST | 清空当前会话 |
| `/api/voice/asr` | POST | 语音识别 |
| `/api/voice/tts` | POST | 语音合成 |
| `/api/spots/ratings` | GET | 获取景点评分汇总 |
| `/api/spots/{spot_id}/reviews` | GET / POST | 查询或提交景点评价 |
| `/api/admin/dashboard/stats` | GET | 管理端数据看板 |
| `/api/admin/conversations` | GET | 对话记录查询 |
| `/api/admin/knowledge/upload` | POST | 上传知识文档 |
| `/api/admin/knowledge/list` | GET | 知识文档列表 |
| `/api/admin/sentiment/*` | GET | 情感分析、趋势、高风险会话、建议报告 |

## 9. 比赛演示流程

推荐演示顺序：

1. **打开游客端首页**：展示景区首页、热门景点、推荐路线和数字人入口。
2. **进入 AI 导游页**：提问“灵山大佛有什么看点？”展示 RAG 问答。
3. **展示路线推荐**：提问“带老人半天怎么玩比较轻松？”展示场景化推荐。
4. **展示语音能力**：使用语音提问并播放 AI 回答，体现 ASR + TTS 链路。
5. **展示景点详情**：查看景点介绍、语音讲解和游客评价。
6. **进入管理后台**：展示数据看板、对话记录、热点话题和情感分析。
7. **展示知识库管理**：上传或查看知识文档，并测试知识库检索结果。

比赛讲解重点：

- 本项目不是普通聊天机器人，而是面向灵山胜境资料的垂直 AI 导览系统。
- 系统通过 RAG 检索增强生成降低景区事实类问题的幻觉风险。
- 游客端服务和管理端运营形成闭环，支持景区持续优化知识库和服务策略。

## 10. 常见问题

### 10.1 后端启动后游客端页面无法访问

检查 `backend/main.py` 中 `web_dir` 是否指向真实的 `web/` 目录。当前推荐部署路径为：

```text
/var/www/Trapezius-driven-development-guide/web
```

如果项目不在该路径，需要修改 `web_dir` 或使用 Nginx 单独托管 `web/`。

### 10.2 语音识别失败

检查：

- 是否配置 `BAIDU_API_KEY` 和 `BAIDU_SECRET_KEY`。
- 本机是否安装 `ffmpeg`。
- 浏览器是否允许麦克风权限。

### 10.3 AI 问答不可用

检查：

- 是否配置 `DEEPSEEK_API_KEY`。
- 网络是否能访问 DeepSeek API。
- 知识库是否已构建，`backend/chroma_db/` 是否存在有效数据。

### 10.4 管理端接口请求失败

检查：

- FastAPI 后端是否已启动。
- `admin-frontend/vite.config.js` 的代理地址是否正确。
- 部署时 Nginx 是否正确转发 `/api/` 到后端。

### 10.5 知识库上传后检索不到

检查：

- 上传文件格式是否为 `pdf`、`docx` 或 `txt`。
- 文档是否包含可提取文本。
- `backend/services/document_indexer.py` 中 ChromaDB 路径是否与实际部署路径一致。

## 11. 当前版本边界

当前版本已完成比赛演示所需核心闭环，但以下能力属于后续增强方向：

- 管理端登录认证、JWT 鉴权和 RBAC 权限控制。
- 生产级 HTTPS、限流、日志监控和自动备份。
- Nginx + 多 Worker + Redis 缓存队列。
- 真实地图导航、票务系统、活动报名和实时客流接入。
- 知识库版本管理、未命中问题池和人工质量反馈。

## 12. 交付说明

比赛上传时建议包含：

- GitHub 仓库地址：`https://github.com/Zz18studying/Trapezius-driven-development-guide.git`
- 完整源码目录。
- `README.md`
- `产品部署与使用说明.md`
- 演示截图或 PPT，如比赛平台要求。

若比赛平台要求“源码从 Git 获取”，可在提交说明中填写：

```text
源码仓库：https://github.com/Zz18studying/Trapezius-driven-development-guide.git
拉取命令：git clone https://github.com/Zz18studying/Trapezius-driven-development-guide.git
```


本项目实际开发在自己购买的服务器http://110.42.246.141/已可以使用，若需要进入服务器开发，可以联系15527590026或电子邮箱2375341456@qq.com